{
  "name": "vp",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@types/webpack",
      "**/@types/webpack/**"
    ]
  },
  "scripts": {
    "api": "yarn workspace @vp/api",
    "web": "yarn workspace @vp/web",
    "common": "yarn workspace @vp/common",
    "dev:api": "yarn api start:dev",
    "dev:api:vite": "yarn api start:dev:vite",
    "dev:web": "yarn web dev",
    "gen:web": "yarn web gen",
    "nest": "yarn api nest",
    "build:api": "yarn api build",
    "build:common": "yarn common build",
    "build:common:watch": "yarn common build:watch",
    "s": "yarn api scaffold"
  }
}
