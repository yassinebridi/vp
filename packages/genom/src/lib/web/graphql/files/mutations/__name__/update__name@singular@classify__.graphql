mutation update<%=singular(classify(name))%>(
  $data: <%=singular(classify(name))%>UpdateInput!
  $where: <%=singular(classify(name))%>WhereUniqueInput!
) {
  update<%=singular(classify(name))%>(data: $data, where: $where)
}
